{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run C++ (Integrated Terminal)",
            "type": "cppdbg",
            "request": "launch",
            "program": "${fileDirname}/${fileBasenameNoExtension}.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${fileDirname}",
            "externalConsole": false,
            "preLaunchTask": "Build active C++ file",
            "MIMode": "gdb",
            "miDebuggerPath": "C:/MinGW/bin/gdb.exe"
        }
    ]
}
